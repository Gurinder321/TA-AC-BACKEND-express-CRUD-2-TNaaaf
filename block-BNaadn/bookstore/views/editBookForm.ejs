<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <nav>
      <a href="/">Bookstore</a>
      <a href="/books">Back</a>
    </nav>
    <div>
      <form action="/books/<%= book.id %>" method="POST">
        <h2>Add Book</h2>
        <input type="text" name="title" placeholder="Add Title" value="<%= book.title %>" />
        <textarea name="description" id="" cols="30" rows="4" placeholder="Add Description">
        <%= book.description %>
    </textarea
        >
        <input type="number" name="price" placeholder="Price" value="<%= book.price %>" />
        <input type="text" name="author" placeholder="Author" value="<%= book.author %>" />
        <button type="submit">Edit Book</button>
      </form>
    </div>
  </body>
</html>
